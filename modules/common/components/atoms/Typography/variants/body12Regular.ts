import { Fonts } from '@fonts';

import { Variant } from './variant-props';

export const body12Regular: Variant = () => ({
  fontSize: 12,
  fontFamily: Fonts.Default,
  fontWeight: '400',
  lineHeight: 18,
});
