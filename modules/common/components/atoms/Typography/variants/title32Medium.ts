import { Fonts } from '@fonts';

import { Variant } from './variant-props';

export const title32Medium: Variant = () => ({
  fontSize: 32,
  fontFamily: Fonts.Medium,
  fontWeight: '600',
  lineHeight: 35.2,
});
