import { palette } from '@palette';

import { Header } from '../components';

export const basicScreenOptions = {
  header: Header,
  headerTitle: '',
  contentStyle: {
    backgroundColor: palette.background.primary,
  },
};
